extends layout 

block content 
    h1= title 

    ul 
        each author in author_list 
            li 
                a(href=author.url) #{author.name}
                | (#{author.formatted_birth} - #{author.formatted_death})
            
        else    
            li There are no authors.